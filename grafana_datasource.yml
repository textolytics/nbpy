apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    database: tick
    user: zmq
    password: zmq
    isDefault: true
    editable: true
